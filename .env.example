# AI 서비스 환경 변수 예제 (TASK-02)
# 실제 값은 이 파일에 적지 않는다. 실제 값은 docs/info.txt(저장소 밖, 사용자 관리)에 보관한다.

# --- LLM / 외부 API 키 ---
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY
SERPER_API_KEY=YOUR_SERPER_API_KEY
UNDETECTABLE_AI_API_KEY=YOUR_UNDETECTABLE_AI_API_KEY

# --- Gemini 모델 폴백 목록 (필요한 만큼 MODEL_NAME0~9 사용, 비워두면 해당 순번 건너뜀) ---
MODEL_NAME0=
MODEL_NAME1=YOUR_PRIMARY_MODEL_NAME
MODEL_NAME2=YOUR_FALLBACK_MODEL_NAME
MODEL_NAME3=

# --- ChromaDB ---
CHROMA_HOST=localhost
CHROMA_PORT=8002
CHROMA_COLLECTION_NAME=legal_documents

# --- Redis (laws_embedding.py 재임베딩 작업 시에만 조건부 필요, TASK-11) ---
# 루트 docker-compose.yml의 redis는 --profile reembed로 켤 때만 뜨고
# requirepass가 걸려 있다. 비밀번호는 루트 .env의 REDIS_PASSWORD와 맞춘다.
REDIS_URL=redis://:YOUR_REDIS_PASSWORD@localhost:6379/0

# --- RunPod 자체 모델 (선택. 비우면 자체 모델 분기를 건너뛰고 Gemini 판정만 사용) ---
RUNPOD_URL=
RUNPOD_API_KEY=

# --- YouTube 쿠키/PO 토큰/프록시 (TASK-07, TASK-16) ---
# 쿠키: 비워두면 저장소 루트의 youtube_cookies.txt를 쓴다. 개인 계정 말고
# 전용(버너) 계정 쿠키를 권장. 프록시를 쓰면 익명으로도 안정적이라 쿠키 생략 가능.
YOUTUBE_COOKIE_PATH=
# PO 토큰: 명시값. 비우면 아래 bgutil 제공자에서 자동 발급.
YOUTUBE_PO_TOKEN=
# bgutil PO 토큰 제공자 서버 (Compose에서 http://bgutil-pot:4416로 주입됨)
BGUTIL_POT_BASE_URL=
# 데이터센터 IP 우회용 프록시. 예: http://user:pass@host:port
# Azure/AWS/GCP 등 클라우드 IP는 YouTube가 자주 차단하므로 주거용 프록시 권장.
YOUTUBE_PROXY=
